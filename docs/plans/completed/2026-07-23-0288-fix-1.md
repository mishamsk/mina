# Plan: 0288 review fixes — store layering and modal edge cases (fix plan 1)

Resolve the operator-review findings on the entry modal without regressing the delivered surface. Kata issue: `0288` (already closed; do not reopen).

## Plan Context

- Do not run review-loop.
- Implementation-only fix plan for review findings on branch `0288-modal-editor`. File:line references are against the current branch head.
- Protect — do not regress (verified live and by 282 e2e):
  - The full EntryModal behavior per `docs/webui-design.md` §3: entry-point unification, deep links (incl. Back-with-dirty-edit guard), Esc ladder, focus-restore chain, batch ergonomics with session rail, bulk-mode force-off, inline-draft discard on open, detail panel staying open beneath and refreshing.
  - Draft persistence for create tabs; sticky fields; session tally.
- Scope exclusions: the Advanced editor's stacked-card layout (pre-existing; the m3ea task owns detail-record presentation questions); evergreen renaming of `EntryPanel`/`openTransactionEntryPanel` identifiers (naming-only churn — leave); the in-app-close history double-entry nit.

## Tasks

### Task 1: Move launch-context sampling out of the store layer

- [x] `frontend/src/store/transaction-entry.ts` no longer reads `window.location`, `document`, testids, or DOM nodes: the launch context (recent-transaction snapshot rows) is computed by the opening caller (feature/page layer) and passed into the open action; `restoreFocusTarget` DOM handling moves to the modal component layer (e.g. a ref owned by the app-shell/modal), so the store holds serializable UI state only.
- [x] `closeTransactionEntryPanel` (`transaction-entry.ts:265-279`) clears the launch-context snapshot and any focus-restore state on close.
- [x] Commit the task as `Pass entry launch context from callers instead of store DOM sampling`.

### Task 2: Modal edge-case fixes

- [x] Duplicate-prefill protection: a modified duplicate launch either persists like create drafts or gets the discard confirmation on close/Back (`entry-panel.tsx:1592-1602,1777-1783`) — silent total loss is impossible.
- [x] Attention strip condition: the footer attention strip appears only when offending fields are actually outside the visible scroll region, and the always-visible general error is not counted as a "field needing attention" (`entry-panel.tsx:1916-1919,3297-3307`).
- [x] `Shift+N` no longer triggers the global new-transaction shortcut (`app-shell.tsx:461-465`).
- [x] Commit the task as `Fix duplicate draft loss, attention strip, and shortcut edge cases`.

### Task 3: Prune dead plumbing

- [x] Remove the orphaned overlay-tracking props (`TransactionBrowser.onDeleteConfirmationOpenChange`, `onRowActionsOverflowOpenChange` at `transaction-browser.tsx:148-149`, toolbar `onFilterBarClose`, filter-controls `onOpenChange`) and the dead `EntryPanel`/`EntryPanelProps` exports in `features/ledger/index.ts:11-13`, updating `frontend/src/features/ledger/PACKAGE.md` only if contract wording references them.
- [x] Commit the task as `Prune dead overlay plumbing and exports`.

## Success Criteria

- [x] Every task's stated outcome and acceptance conditions are complete.
- [x] `just pre-commit` passes.
- [x] `just test-frontend-e2e` passes.
- [x] Planned commits are present and the worktree is clean (`design-prototypes/` stays untracked and uncommitted).
- [x] Move this plan to `docs/plans/completed/` and commit the move.
