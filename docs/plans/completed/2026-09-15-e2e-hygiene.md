# Plan: Frontend e2e hygiene sweep (feedback fleet, Brief F)

## Goal

Every frontend e2e test added or extended since `main` by the 2026-09-13 P1/P2 fleet and by this fleet complies with `docs/FRONTEND-TESTING.md`: regression-style tests for small fixes, matrices, exact-geometry assertions, REST-shape guards, and dialog-internals sweeps are removed, and the essential visible assertions are folded into the journey that already owns each surface. The suite ends at 137 tests or fewer (main has 136; the one allowed addition is the keyboard-help journey).

## Constraints

- No Kata issue; this is Brief F of `docs/plans/2026-09-14-p1-p2-feedback-fleet.md`.
- Testing policy: `docs/FRONTEND-TESTING.md` governs every edit. No new tests, no new files except one shared helper module; every fold keeps only the assertions named below; no `page.request` as action or evidence (fixture setup only); no `boundingBox` equality; no fixed waits; no key or state matrices; `page.reload()` only where persisted restore is the behavior under test.
- Baseline counts: main 136 tests (272 runs); HEAD 147. Target after this sweep: 137 or fewer, every spec at or under 25.
- Required edits (file paths under `frontend/tests/e2e/`):
  - `keyboard-shortcuts.spec.ts` (3 → 1): keep the first journey (`?` opens the dialog with route-scoped sections, Escape restores row focus) and fold into it exactly: one "`?` typed in a text field types the character and opens nothing" assertion, the "Transaction entry" heading visible when help is opened over the entry modal, and "focus returns to the pre-palette invoker" after opening help through the palette action. Remove the 500px viewport ArrowDown/End/Tab viewport sweep, the tooltip-hidden check, the overlay-click re-open leg, the Ctrl+K-inert-in-entry leg, and the other two tests.
  - `reference-table-layout.spec.ts` (6 → 3): remove the Recurring and Categories overflow tests and the multi-currency account test; add one `expectNoDocumentVerticalOverflow(page)` line to the existing Accounts/Categories internal-scroll journey; drop the redundant `scrollTop > 0` evaluate after the `toBeInViewport` assertion in the scroll helper.
  - `accounts-page.spec.ts` (13 → 12): remove the canvas-scroll/pagination block and its 28-account REST loop from the group-register journey; fold into the existing "register stays usable across representative widths" journey exactly two lines after paging: footer `toBeInViewport()` and `expectNoDocumentVerticalOverflow(page)`; remove the settlement-dates `describe` and fold one assertion into `lifecycle-timezone.spec.ts` (navigate to the funding account register and assert the pending row's date cell shows the settlement day with a `toHaveText(/Jul 24/)`-style regex); keep the Home/End roving fold.
  - `categories-page.spec.ts` (7 → 6): remove the economic-intent geometry test; keep the roving-row fold but drop its redundant duplicate `toBeVisible` line.
  - `command-palette.spec.ts` (7 → 7): in the account-actions journey keep Enter → register and Cmd/Ctrl+Enter → filtered Transactions URL; keep one ribbon `toContainText("Cmd/Ctrl Enter opens filtered Transactions")`; remove the held-modifier duplicate, the Edit-mode leg, and three of the four `aria-selected` assertions.
  - `recurring-page.spec.ts` (12 → 10): remove the backlink history-sweep test and fold one assertion into `transactions/detail.spec.ts` backlink journey (after navigating away and `goBack()`, the Definition FQN field holds the fixture FQN); remove the compact Create-recurring test and fold into `transactions/transaction-actions.spec.ts` "row overflow opens a seeded recurring definition" exactly `editor toHaveCount(0)` and `alertdialog toHaveCount(0)` after a nav link click; trim the dirty-guard journey to `keepEditing toBeFocused()` only (drop the Tab/Shift+Tab sweep); drop the duplicated second "View transactions" tail in the drill-down journey.
  - `transactions/entry.spec.ts` (15 → 14): merge the active-day seeding test and the required-date test into one journey keeping: date `toHaveValue("2026-08-12")` on open from an anchored Transactions list, "Date is required." visible with the Date field focused on one tab, and date `toHaveValue("")` when the saved draft reopens on Overview; remove the template-apply and Clear-draft legs there, the Advanced repeat, the attention-summary regex, and the `n`+`i` chord tail (chords are covered elsewhere); in the explicit-save journey drop one of the two identical discard legs; in the dirty-edit journey keep only the Discard-focused assertion and drop the Escape repeat; remove the unrelated-draft leg from the duplicate journey; replace the hardcoded negative date assertion in the batched journey with the real expectation or drop it; replace the palette-button readiness locator with an assertion on the page heading.
  - `transactions/filtering.spec.ts` (8 → 8): in the confirm-expected journey keep `confirm toBeDisabled()` plus the tooltip text; drop the Cancel-Tab focus step and the Enter/Space non-activation pair.
  - `status-page.spec.ts` and `tags-page.spec.ts` one-line focus folds: keep.
  - Shared helper: move `expectNoDocumentVerticalOverflow` (and a horizontal twin if any inline copy survives) into a module beside `frontend/tests/e2e/test.ts`; remove the inline copies.
- Do not touch tests that predate the fleets except where a fold above lands in them; do not consolidate the pre-existing per-spec fixture creators (out of scope).
- Record the before/after test count per spec in the commit body.
- Docs: `docs/FRONTEND-TESTING.md` is unchanged; no package docs change.
- Mina is pre-production with no users.
- Do not edit this plan except to tick checkboxes and to move it to `docs/plans/completed/`.

## Success Criteria

- [x] Per-spec counts match the targets above and the total is 137 or fewer; `grep -rn "waitForTimeout\|boundingBox" frontend/tests/e2e` shows no new geometry equality or fixed waits.
- [x] `just frontend-check` passes.
- [x] `just test-frontend-e2e` passes (full suite) on chromium and webkit.
- [x] `just pre-commit` passes.
- [x] From a clean worktree, run `just review-loop --plan "docs/plans/2026-09-15-e2e-hygiene.md"` exactly once, resolve its findings, and rerun affected validation. Never invoke review-loop a second time, even if findings remain; report any remaining findings in the completion report. Reviewers must not request added coverage or restored tests.
- [x] Move this plan to `docs/plans/completed/`, commit the move, and leave the worktree clean.

## Tasks

### Task 1: Shared overflow helper and layout/keyboard specs

- [x] `keyboard-shortcuts`, `reference-table-layout`, `accounts-page`, `categories-page`, `lifecycle-timezone` edits complete with the shared helper.
- [x] Commit as `test(frontend): fold layout and keyboard-help coverage into existing journeys`.

### Task 2: Palette, recurring, transactions specs

- [x] `command-palette`, `recurring-page`, `transactions/detail`, `transactions/transaction-actions`, `transactions/entry`, `transactions/filtering` edits complete; counts recorded in the commit body.
- [x] Commit as `test(frontend): consolidate palette, recurring, and entry journeys per testing policy`.
