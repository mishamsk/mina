# Plan: Focus transaction entry on the first empty field — cwxv

## Goal

Create launches focus the first empty editable field in the active transaction form, with Date as the fallback when every field is populated. Modal-scoped `Mod+L` focuses and opens compatible template search without changing global list-search behavior. Preserve template application, draft choices, the Esc ladder, close restoration, and phone takeover. Kata: `cwxv`.

## Constraints

- Frontend-only; no Go or OpenAPI changes. Do not modify the global list-search handler or `globalShortcutGroup`; do not touch `VISION.md`, `SCOPE.md`, `docs/architecture.md`, or `docs/frontend-architecture.md`. Keep `docs/webui-design.md` edits to the Keyboard entry-modal shortcut clause and Transaction entry “Close / Esc / drafts / focus” final clause.
- `docs/FRONTEND-TESTING.md` governs. No new e2e test for a bug fix or small behavior change; fold a visible assertion into the existing journey that already exercises that surface, or add nothing.
- No REST calls as the action or evidence under test; fixture-only API setup is allowed when it keeps a journey short. No per-state or per-route matrices, no exact-geometry assertions, no fixed waits.
- Net e2e test count may only go down or stay equal for this task (baseline: 137 `test(` declarations under `frontend/tests/e2e`, counted with `rg -n '^test\(' frontend/tests/e2e`).
- Do not add e2e tests; reviewers must not request coverage; fold any missing assertion into an existing journey. The only expected test edit: the initial-focus assertion in `transactions/entry.spec.ts` changes to the new target (and at most one `Mod+L` → picker-focused assertion folded into that same journey or an existing template-picker journey). No new `test(` blocks.
- Before authoring a subplan, the operator reads the relevant `docs/webui-design.md` sections (Keyboard, Tables and filtering, Transaction entry) and `docs/webui-theme-arcade-cabinet.md`, and the planning prompt requires a short design spec in the subplan: placement, states, geometry stability (no layout jumps on focus, hover, or modifier state), theme tokens, keyboard and focus behavior.
- Implementor screenshots of the changed surface are required in the completion report; the operator judges them against the theme doc before merge. Anything jumpy, re-ordering, or styled off-theme is a fix-plan defect, not a nit.
- Focus visuals follow the existing entity-list contract: roving-tabindex row navigation with hover-style row highlight; no focus ring on non-editable surfaces.
- Use `write-package-docs` for every package touched; update only affected contracts. Behavior and corresponding `PACKAGE.md`, `PROJECT_STATE.md`, and `webui-design.md` statements land together. Keep one evolving PROJECT_STATE clause for this outcome.
- Run `just pre-commit` and `just test-frontend-e2e` before each implementation commit; no backend suites. Use Justfile recipes only, including `just prose-fmt` for implementation documentation. The docs-only completed-plan move needs no tests or broad validation.
- Close every `agent-browser` session opened, including named `--session` sessions, before finishing; include screenshot paths in the completion report.

## Success Criteria

- [x] New buttons, `?entry=new[:tab]` (`journal` selects Advanced), `n` chords, palette entry commands, template Use/palette template launches, and restored saved drafts share the first-empty rule in `frontend/src/features/ledger/entry-panel.tsx`; edit/split/duplicate launch at Date. Optional empty fields count; all-populated forms fall back to their first editable field. Initialization neither opens pickers nor steals focus after interaction.
- [x] Eligible `Mod+L` focuses and opens Start from a template from any panel control, including text inputs and an already-focused picker; unavailable/empty/Exchange/confirmation states leave the event unconsumed. Help distinguishes Transaction entry’s template action from mounted List search’s page action.
- [x] Existing template replacement, Save draft/Discard choices, Esc ordering, save-and-add-another Date focus, close restoration, and compact takeover remain intact; stage geometry stays stable and only necessary body scrolling occurs.
- [x] `just pre-commit` and `just test-frontend-e2e` pass before each implementation commit; `rg -n '^test\(' frontend/tests/e2e` still reports no more than 137 declarations. No additional test blocks, suites, or coverage requests.
- [x] Completion report includes screenshots immediately after plain `n` on Transactions (`Funding account` focused with the seeded date) and Overview (Date focused with no seed), their paths, browser checks, and any limitations/findings. All browser sessions are closed; the operator checks screenshots against the theme before merge.
- [x] From a clean worktree, run exactly one `just review-loop --plan "docs/plans/2026-09-17-cwxv-entry-first-empty-focus.md"`. Resolve actionable findings within scope, commit fixes, and rerun affected validation; never rerun review-loop, even if findings remain. Report leftover findings in the completion report; reviewers must not request coverage.
- [x] Close Kata with the actual final implementation SHA and evidence: `kata close cwxv --done --message "Implemented first-empty entry focus and compatible-template Mod+L; verified browser behavior and required screenshots" --commit <sha> --test "just pre-commit; just test-frontend-e2e" --agent`.
- [x] Move this plan to `docs/plans/completed/2026-09-17-cwxv-entry-first-empty-focus.md`, commit as `docs: complete cwxv entry focus plan`, and leave the worktree clean.

## Tasks

### Task 1: Focus the active create form without opening a picker

Verified evidence, relative to the current branch:

- `frontend/src/features/ledger/entry-panel.tsx:3375` owns initial focus; `:3192` gates draft readiness. `frontend/src/features/ledger/entry-modal.tsx:427` captures the restore target and initially focuses the dialog; its `:285` overlay marker, `:316` Esc guard, and `:296` close restoration stay intact.
- `entry-panel.tsx:325` defaults shorthand Currency to USD and bought currency to EUR; `:354` defaults Advanced row currency to USD; `:378` creates two blank Advanced rows. `frontend/src/features/ledger/entry-launch-context.ts:46` supplies dates only on Transactions, from its selected day or local today. Draft interfaces are in `frontend/src/models/ui-state.ts:24`, `:48`, `:72`, and `:79`; do not change persistence shapes.
- `entry-panel.tsx:5209` renders Advanced Date before row account/amount/currency/category/tags/member controls (`:5331` onward). Shorthand Date/Currency/Amount begin at `:5825`; primary account at `:5932`; Spend merchant rows follow. Correction to supplied research: Exchange renders sold amount/currency, primary and secondary accounts, then bought amount/currency (`:6185` onward), so derive order from live DOM rather than a copied field list.
- `entry-panel.tsx:1190` and `:3047` compare dirty drafts, not field emptiness. `:3691` provides an existing DOM-focus pattern, but its error scrolling is not the initial-focus policy. Save-and-add-another Date focus at `:4679` and `:4969` remains unchanged.
- Correction: duplicates explicitly have `replacement: undefined` at `entry-panel.tsx:1617` and `:1640`, so the current effect actually takes the template-focus branch. The requested Date outcome requires recognizing edit/split/duplicate by launch identity rather than relying solely on `replacement`; keep their submission and draft semantics unchanged.
- `entry-panel.tsx:4051` applies templates and calls `focusTemplatePicker`; `:4194` schedules initial template application after draft readiness. Initial template application must finish, including any replacement choice, before the launch's form-focus attempt; ordinary in-session template application and Clear retain their existing focus behavior.

Design spec:

- Placement: no new UI. The Date input (or whichever field is first-empty) receives the standard theme focus ring (2px `--ring` `#D1179E`, 2px offset) on the white modal surface; no other visual change. Screenshot required: the modal immediately after `n` on Transactions (seeded date → expected first-empty field focused) and after `n` on Overview (unseeded → Date focused). Existing tokens/rules are in `frontend/src/styles.css:93` and `:196`; use them without component-specific styling.
- Geometry stability: the stage frame does not move or resize when focus lands; no body scroll jump; the template picker popup does not open on initial focus (it opens only on `Mod+L` or on user focus of the picker). Preserve existing user-driven arrow-key opening as part of the picker contract. If the target and ring already fit inside the body viewport, focus with `preventScroll`; if offscreen, reveal it with the smallest necessary scroll in `entryScrollRegionRef` (including the Advanced horizontal region where needed), then focus after visibility is established. Never scroll the page or reposition the pinned frame, and never use unconditional `preventScroll` to leave an offscreen field focused.
- States: Exchange tab → `Mod+L` inert; tabs with zero compatible templates → inert; a confirm dialog open → inert; while the picker is already focused → re-open it (idempotent).
- Keyboard: `Mod+L` opens the picker with its existing keyboard contract (arrows/Enter/segment keys); Esc closes the popup first (existing ladder); Tab order unchanged.

Expected first targets for otherwise blank drafts with existing defaults; this is a specification table, not a test matrix:

| Active tab | Unseeded launch, including Overview | Transactions seeded-date launch |
| --- | --- | --- |
| Spend | Date, `#spend-date` | Funding account, `#spend-fundingAccountId` |
| Income | Date, `#income-date` | Amount, `#income-amount` |
| Refund | Date, `#refund-date` | Amount received, `#refund-amount` |
| Transfer | Date, `#transfer-date` | Amount, `#transfer-amount` |
| Exchange | Date, `#exchange-date` | Amount sold, `#exchange-amount` |
| Advanced | Date, `#advanced-date` | Record 1 account, `#advanced-record-0-account` |

- [x] In `frontend/src/features/ledger/entry-panel.tsx`, walk the active tab's form body in document order for `input`, `textarea`, `[role='combobox']`, and `select`. Exclude the template header, action buttons, checkbox/radio/toggle inputs, disabled/read-only/inert controls, hidden ancestors, and unrendered conditional fields. Include offscreen editable fields inside the scroll region; “visible” must not mean “already inside the viewport.” Focus the first empty candidate, otherwise the first candidate (Date).
- [x] Define emptiness by control kind: native text/date/amount/textarea/currency-datalist values are empty when their value is blank; numeric zero is populated; select is empty only at its empty placeholder value; a single entity picker is empty when its selected ID is absent; multi-pickers are empty when both selected IDs and group selections are empty. A typed query/breadcrumb is not a selection, and unresolved selected IDs still count as populated. Expose minimal selection metadata from `frontend/src/features/ledger/entity-picker.tsx` if needed; do not infer selection from display text or treat the cleared multi-picker input as empty when chips are selected. A cleared Currency may therefore precede Amount/account in a partially populated draft.
- [x] Coordinate one launch-scoped focus attempt with hydrated/adopted drafts and initial template completion in `entry-panel.tsx`. Defer for every confirmation, reject superseded launches, and preserve user focus after interaction; tab changes, lookup refreshes, availability responses, and ordinary draft edits must not retrigger initial focus. For template Use, use the resolved compatible tab and applied values, retaining replacement confirmation and cancellation semantics.
- [x] Add the smallest necessary programmatic focus control in `frontend/src/features/ledger/entity-picker.tsx`, including `EntityMultiPicker`, so initial form focus suppresses opening exactly once while normal later focus opens normally. A restored draft may land on any picker, including Tags/Member. Keep `entry-modal.tsx` restore ownership, the phone frame, and native datalist behavior unchanged.
- [x] In `frontend/tests/e2e/transactions/entry.spec.ts:533`, replace the existing template-focus assertion in “transaction entry guards focus and remains usable on a phone” with `editor.getByLabel("Funding account")` focused. Preserve the separate Clear-draft template-focus assertion at `:178`.
- [x] Evidence-driven journey maintenance: `frontend/tests/e2e/command-palette.spec.ts:200` currently asserts the template popup is automatically expanded, then uses two Esc presses. Insert a user focus/click of its existing `templatePicker` before that existing expanded assertion so the same popup/Esc journey remains valid; add no assertion or test. This necessary setup adjustment is the only additional edit beyond the expected focus assertion; `frontend/tests/e2e/transactions/entry-pickers.spec.ts:62` already explicitly fills the picker and needs no rewrite.
- [x] Update only the final focus clause at `docs/webui-design.md:282`: creates focus the first empty editable field in active-form document order, falling back to its first field; edit/split/duplicate focus Date. Through `write-package-docs`, replace the obsolete template-initial-focus claim and update the entry-draft focus contract in `frontend/src/features/ledger/PACKAGE.md`; add one concise clause to the existing transaction-entry statement in `PROJECT_STATE.md`.
- [x] Use browser controls for a short manual check of blank/seeded and restored or template-prefilled entry, confirming that initial picker targets remain closed and visible without moving the frame. Capture `build/cwxv/transactions-entry-focus.png` and `build/cwxv/overview-entry-focus.png` immediately after `n` with no saved draft; retain the existing phone journey for compact evidence. Close browser sessions, run the required per-commit validation, and inspect the diff for unintended changes.
- [x] Commit as `fix(ledger): focus transaction entry on its first empty field`.

### Task 2: Open compatible template search with modal Mod+L

Verified evidence and chosen availability approach:

- `frontend/src/features/ledger/entry-panel.tsx:3169` creates a compatible-template loader per active tab, absent on Exchange and unrestricted by shorthand on Advanced; `frontend/src/features/ledger/entity-picker-loaders.ts:320` delegates to `searchTransactionTemplates` and maps bounded `items`/`has_more` to rows/hasMore. Keep that REST contract and limit unchanged.
- `frontend/src/features/ledger/entity-picker.tsx:507` defers closed non-autofocus loads. Its `:554` callback reports only returned leaves, omitting hierarchy groups and query scope; neither that callback alone nor the current query's empty result proves the tab has no compatible templates. There is no existing panel availability read to reuse. Add an opt-in empty-query/root preload to the existing picker load pipeline, enabled only for the ready template picker, and expose its scoped availability to the panel. Reuse that result/in-flight read on first open; do not add a separate panel REST request or fetch the complete template list.
- Correction: `entry-panel.tsx:3079` currently sets `openOnFocus` false around focusing and then restores true. `entity-picker.tsx:1158` checks it only in `onFocus`; restoring the prop neither opens a focused picker nor fires another focus event. Reuse `focusTemplatePicker` with an explicit open intent for the shortcut, using a narrow picker API that works while already focused; preserve focus-only behavior for its existing Clear/template/error/confirmation call sites.
- `entry-panel.tsx:5042` already handles `Mod+S` and submit after four confirmation guards, with no editable-target exclusion. `entity-picker.tsx:1192` yields modifier chords to the panel. `frontend/src/features/app-shell/app-shell.tsx:941` keeps global list search suppressed by the modal marker; no global changes are needed.
- Help is registration-based, not filtered by overlay visibility: `frontend/src/features/app-shell/keyboard-shortcuts-dialog.tsx:15` renders Global plus mounted groups, with z-index 75/76 above entry's 70. `app-shell.tsx:1146` keeps the route outlet mounted; `frontend/src/features/ledger/transaction-search-input.tsx:16` registers List search for its mounted lifetime, through `frontend/src/features/app-shell/use-list-search-shortcuts.ts:19`. On ordinary Transactions entry, expected order is Global, Transaction entry (order 2), List search (order 5), Transactions (order 10), plus any other mounted detail groups. Overview has no List search owner. Opening entry alone does not unregister List search; an underlying owner must be absent/unmounted for that group to disappear. Help remains available via `?` from a non-editable modal control after closing picker overlays.

- [x] In `frontend/src/features/ledger/entity-picker.tsx`, publish availability from a successful current-lifecycle empty-query/root result: a leaf or a navigation group with compatible descendants means available; an empty successful result means unavailable. Preserve this root fact when later queries or drilled scopes have no matches. Unknown/loading/failed availability is inert; retries use existing picker behavior. Clear/remount/tab/session transitions must invalidate stale results; preload must not focus, open, scroll, or steal focus. Keep ordinary pickers' lazy loading unchanged and reuse the picker result rather than issuing a second fetch per tab.
- [x] In `frontend/src/features/ledger/entry-panel.tsx`, handle `(event.metaKey || event.ctrlKey)`, lowercase `l`, no Alt/Shift in the existing section handler. After confirmation guards, require a current enabled template picker and positive active-tab availability; Exchange, zero matches, unknown/failed availability, missing picker during edit/split, saving/clearing, and confirmation states return without consuming the event. When eligible, call `preventDefault()` before `focusTemplatePicker` with explicit open intent so the browser address bar never takes the accepted chord. It applies inside text inputs just like existing Mod+S; do not copy the global handler's editable-target exclusion. Ineligible chords retain their native browser behavior.
- [x] In `frontend/src/features/ledger/entry-shortcuts.ts`, add a unique entry shortcut ID with keys `["Mod", "L"]` and label “Start from a template” under existing group `entry`, order 2; add a concise availability detail if useful. Preserve `globalShortcutGroup` and List search registration/semantics. Keep the entry catalog registered throughout modal openness, including on Exchange; runtime eligibility is distinct from catalog membership.
- [x] Optionally fold at most one `Mod+L` → template-picker-focused assertion into the existing journey in `frontend/tests/e2e/transactions/entry-pickers.spec.ts`, which already creates a compatible template, or the existing phone journey in `transactions/entry.spec.ts`; no new test blocks or other test expansion. Use normal visible UI readiness, never request waits or fixed delays.
- [x] Manually check the accepted chord from a text field, already-focused picker reopening after Esc, and inert Exchange/empty/confirmation behavior; exercise normal picker keys and the existing popup-first Esc ladder. Open help from a non-editable modal control, confirm the catalog described above and restoration back into entry, then close entry to its opener. Do not automate a per-state/route matrix or change global shortcut dispatch.
- [x] Add `Cmd/Ctrl+L` with its compatible-template condition only to the entry-modal shortcut clause in `docs/webui-design.md:177`; update the affected picker/entry contract in `frontend/src/features/ledger/PACKAGE.md` through `write-package-docs`, and extend the same `PROJECT_STATE.md` clause from Task 1 with template shortcut availability. No app-shell/hooks code or documentation changes are expected.
- [x] Close browser sessions, run the required per-commit validation, and confirm the e2e declaration count has not increased. Preserve screenshots and their paths for the final report, then follow the plan-wide single-review and completion workflow.
- [x] Commit as `feat(ledger): open compatible templates with modal Mod+L`.
