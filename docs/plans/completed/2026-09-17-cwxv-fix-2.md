# Plan: Settle entry initial focus and preserve picker click caret — cwxv fix 2

## Goal

Complete the second and final localized fix on `cwxv-entry-first-empty-focus`: reveal and focus entry targets in the same attempt, settle exhausted initial-focus work, preserve picker caret placement, and keep the existing focus ring visible. Kata `cwxv` is closed; the prior plans remain at `docs/plans/completed/2026-09-17-cwxv-entry-first-empty-focus.md` and `docs/plans/completed/2026-09-17-cwxv-fix-1.md`.

## Constraints

- Do not run review-loop.
- `docs/FRONTEND-TESTING.md` governs. Do not add e2e tests; reviewers must not request coverage; fold any missing assertion into an existing journey. Net e2e `test(` count stays at most 137 (`rg -n '^test\(' frontend/tests/e2e`).
- One implementation task and one implementation commit, limited to S1–S4 and N5–N8 below. Changes stay in `frontend/src/features/ledger/entry-panel.tsx`, `entity-picker.tsx`, `entry-shortcuts.ts`, and the ledger `PACKAGE.md`, plus existing frontend journeys only if a missing visible assertion needs folding in. No new dependencies, backend/API/persistence changes, global shortcut changes, or unrelated cleanup.
- Keep Kata `cwxv` closed and both archived plans unchanged. Do not change product, architecture, theme, or project-state documents. Use `write-package-docs` for the touched ledger package and replace only affected contracts.
- Use Justfile recipes only for development startup, formatting, checks, and tests. Run `just prose-fmt` for implementation documentation, then `just pre-commit` and `just test-frontend-e2e` before the implementation commit. No backend suites: do not run `just test` or `just test-integration`. The later docs-only plan-move commit requires no tests or broad validation; `just prose-fmt` excludes `docs/plans/`.
- Keep browser checks short and driven by normal UI controls and visible results. No timing/failure injection, isolated implementation tests, request synchronization, fixed waits, exact-geometry assertions, or new automated state/route matrix. Inspect retry exhaustion and missing-handle guards in code.
- Close every `agent-browser` session opened, including named sessions, before finishing. Report screenshot paths for Overview `n` → Date focused and compact (390px) Transactions `n a` → first Advanced record account focused with its ring visible.

## Success Criteria

- [x] A connected, owned offscreen or oversized target receives nearest native scrolling and focus in the same attempt, without a popup; exhaustion settles the launch so later dependency changes cannot restart it. Missing picker handles retry and settle without raw focus. Readiness, confirmation, launch identity, and user/conflict cancellation guards remain effective.
- [x] Clicking an already-focused picker preserves caret/selection and active row while opening or keeping open its popup. First user focus and explicit imperative open retain their seeding behavior; handle identity stays stable across query/options/selection updates.
- [x] Preserve the operator-verified launch outcomes with short manual checks: Overview `n` → Date, no popup; Transactions `n` → Funding account with seeded Date; Transactions `n i` → Income amount, `n t` → Transfer amount, and `n a` → first Advanced record account; Edit → Date; unseeded Templates Use → Date; restored draft → next empty field; compact Transactions `n` → Funding account, no popup or page scroll. Preserve split/duplicate Date focus, first-field fallback, optional-field emptiness, and save-and-add-another focus through unchanged code paths.
- [x] Preserve `Mod+L` consumption on every rendered/enabled create tab including Exchange (`defaultPrevented === true`); Spend opens template search, including from Memo; Esc closes only the popup and a second `Mod+L` reopens it. Confirmation and absent/disabled-picker guards remain intact. Funding focus opens, Esc closes, and clicking the focused-but-closed picker reopens; ordinary template-editor pickers still open on focus. `?` from a non-editable modal control lists “Start from a template” under Transaction entry, and modal closure restores its opener.
- [x] Completion evidence reports browser findings and any limitations, plus `build/cwxv-fix-2/overview-entry-focus.png` immediately after Overview `n` and `build/cwxv-fix-2/compact-advanced-entry-focus.png` immediately after Transactions `n a` at 390px width. Both show the correct focused field, no initial popup, a visible standard ring, and stable pinned frame; all opened browser sessions are closed.
- [x] Existing phone-journey and command-palette setup edits remain valid, the e2e declaration count stays at most 137, and `just pre-commit` and `just test-frontend-e2e` pass before the implementation commit.
- [x] After the implementation commit, mark this plan complete, move it to `docs/plans/completed/2026-09-17-cwxv-fix-2.md`, commit the move as `docs: complete cwxv fix 2 plan`, and leave the worktree clean.

## Tasks

### Task 1: Settle initial focus attempts and preserve picker click caret

Evidence verified at `0986891c` against current code and `git diff e318530a..HEAD`. Below, `entry-panel.tsx`, `entity-picker.tsx`, `entry-shortcuts.ts`, and `PACKAGE.md` refer to `frontend/src/features/ledger/`. The current e2e count is 137. The phone journey at `frontend/tests/e2e/transactions/entry.spec.ts:530–536` checks 390px Funding focus and click-to-open; `frontend/tests/e2e/command-palette.spec.ts:197–201` explicitly clicks the template picker before checking expansion.

Design spec: preserve the white modal surface, pinned header/footer, compact takeover, existing field/tab order, and popup placement. Keep the theme's 2px `--ring` outline and 2px offset; add only small entry-scoped Tailwind scroll margins. Native nearest scrolling may reveal the target inside the entry body, including Advanced horizontal overflow, while the page and pinned frame stay fixed. Focus, hover, and modifier state must not resize or reorder the layout. Initial focus keeps pickers closed; normal focus, click, explicit open, arrows, and Esc retain their established behavior except for the corrected click reseeding.

- [x] **S1:** `entry-panel.tsx:2901–2912` requires full four-edge containment; `:3611–3618` scrolls, retries, and returns before focus, consuming the budget initialized by `:3639` (`scheduleAttempt(4)`). No smooth-scroll rule exists under `frontend/src`; `styles.css:1378` specifies only reduced-motion `auto`. Remove the post-scroll retry/return and fall through to focus in the same attempt. Replace full containment with an intersection-tolerant visibility check, or remove that check and call nearest native scrolling directly; oversized fields must not be required to fit fully. Preserve connected-target/body ownership checks, guard checks before scrolling/focusing, and `preventScroll: true` on focus. Do not introduce a new scrolling abstraction or increase the retry budget.
- [x] **S2:** `entry-panel.tsx:3570–3574` drops exhausted retries while leaving session status pending. Call `settleFocus()` when the current launch exhausts its retries; preserve generation and cancellation protection. Confirm by code inspection that missing refs, disconnected targets, missing handles, and failed focus all terminate through the bounded path, and a settled launch cannot restart after dependency changes.
- [x] **S3:** `entry-panel.tsx:3624–3632` falls back to `focusWithoutTooltip` when a handle is absent, which triggers the picker’s ordinary focus-open handler at `entity-picker.tsx:1279–1284`. All 14 current single/multi-picker sites register their handles, from `entry-panel.tsx:5425–5427` through `:6684–6686`; preserve those registrations and cleanup at `:3052–3067`. If a target carries `data-entity-picker-input` but has no registered handle, retry as not yet mounted instead of raw-focusing it. Keep raw focus available for native non-picker fields and settle only after actual focus success or terminal exhaustion/no candidates.
- [x] **S4:** `entity-picker.tsx:1289–1291` calls `openFromFocus(false)` on every click; `:772–790` selects existing text and resets the active row. When `document.activeElement === inputRef.current`, click should only call `updateOpen(true)` without select-all or active-row reseeding. Keep first-focus seeding at `:1279–1284` and explicit imperative-open seeding. Verify ordinary caret placement in a populated picker while open and after Esc, plus focused-but-closed Funding click-to-open; the shared fix must apply to filters, template/recurring editors, edit dock, and entry without consumer-specific changes.
- [x] **N5:** `entry-panel.tsx:3103–3108` and `:3309–3328` mirror focus-effect dependencies into `initialFocusConditionsRef`; `:3540–3550` reads them, and `:3640–3656` cancels the frame on dependency cleanup. Remove the mirror and use effect closure values while keeping the live session and user-focus guards. If a concrete interleaving requires the mirror, retain it only with a one-line comment naming that interleaving; do not preserve it for hypothetical safety or weaken cancellation.
- [x] **N6:** `entity-picker.tsx:805–829` recreates its imperative handle whenever `openFromFocus` changes with query/options/selection (`:792–802`). Keep the latest `openFromFocus` in a ref and invoke it through the handle; use stable dependencies such as `[id, disabled]`. Preserve fresh seeding values, disabled behavior, synchronous suppression with `finally`, actual focus-success reporting, and unmount cleanup.
- [x] **N7:** Replace `entry-shortcuts.ts:27` with `Opens when compatible templates exist.`; preserve the label and Transaction entry group. Use `write-package-docs` to update only the affected ledger contracts at `PACKAGE.md:40` and `:42`: exhausted attempts settle, missing picker handles retry, nearest native scrolling precedes focus, and already-focused clicks preserve caret/active row rather than reseeding. Keep the enabled-create consumption and preload lifecycle contracts intact.
- [x] **N8:** Neither `entry-panel.tsx` nor `entity-picker.tsx` has a scroll-margin utility. Add a small Tailwind margin such as `scroll-m-1` to entry focus targets, directly or via an entry-scoped descendant utility on their container, so it applies to the actual element being scrolled. The body is at `entry-panel.tsx:5475–5484` and the first Advanced account input is composed at `:5613–5623`. Verify its ring is visible at 390px and that already-visible fields cause no unnecessary scroll. No other styling changes or JavaScript ring-margin arithmetic.
- [x] Perform the protected manual checks and capture the two screenshots specified above using the `agent-browser` skill; close all sessions. Inspect guard/callback paths and the final diff, retaining the existing phone and palette journeys; fold only a missing essential visible assertion into an existing journey if necessary.
- [x] Run `just prose-fmt`, then `just pre-commit` and `just test-frontend-e2e`; confirm `rg -n '^test\(' frontend/tests/e2e` still lists at most 137 declarations and record results before committing.
- [x] Commit as `fix(ledger): settle entry initial focus attempts and keep picker click caret`.
