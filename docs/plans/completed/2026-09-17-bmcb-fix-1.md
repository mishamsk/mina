# Plan: Correct table-entry readiness and list-search shortcut scope — bmcb fix 1

## Goal

Fix the nine audited frontend defects on `bmcb-table-focus-search-shortcut`, preserving the design spec in [the archived bmcb plan](completed/2026-09-17-bmcb-table-focus-search-shortcut.md) except where this fix plan explicitly corrects it. Entry focus waits for a settled result, entity editors block only list-search dispatch from their focused controls, and shortcut wiring, documentation, and existing journey assertions remain concise. Kata `bmcb` is already closed; leave its status unchanged. Execute the two implementation tasks sequentially.

## Constraints

- Do not run review-loop.
- `docs/FRONTEND-TESTING.md` governs. Do not add e2e tests; reviewers must not request coverage; fold any missing assertion into an existing journey. Net e2e `test(` count stays at most 137.
- Frontend only: no Go, REST/OpenAPI, store-lifecycle, styling, or unrelated product changes. Do not revisit the Shift-produced `/` layouts limitation. Do not change `docs/webui-design.md` beyond anything strictly required by the side-panel correction; the verified design already supports the correction, so no edit is expected.
- Preserve palette, `n` chord, and `?` dispatch behavior and the recurring-definition editor's existing global blocking. Leave `globalShortcutGroup`, help group ordering, and shortcut-store structure untouched.
- Preserve one focus attempt per entry identity, including group-prefix identity, empty-result consumption, live eligibility checks, cleanup cancellation, and preview/Status opt-outs. Refreshing, paging, filtering, and sorting never re-arm the attempt; do not add another autofocus or restoration path.
- Retain the archived design's placement and visual states: existing table rows and search inputs, hover-fill row focus without a ring, existing search-input ring, no geometry changes or new theme tokens. Row entry focus keeps `preventScroll: true`; shortcut-driven search focus may scroll the field into view.
- Use `write-package-docs` for every touched frontend package; update only changed implicit contracts in the same implementation commit. Keep prose paragraphs and bullets on one physical line and run `just prose-fmt` during implementation. Preserve unrelated work.
- Validation: run `just pre-commit` and `just test-frontend-e2e` before each implementation commit (not the docs-only plan-move commit). No backend suites (`just test` or `just test-integration`); the frontend recipe's required build remains enabled. Run formatting and validation through Justfile recipes only.
- The implementor must close every `agent-browser` session it opens, including named `--session` sessions, before finishing. Retain focused-row and shortcut-focused-search screenshot paths and report validation results, commit SHAs, and any limitations.

## Success Criteria

- [x] All six entry-focus sites remain unready before any result exists, wait while loading, and consume their single attempt after a settled result, including an empty result or a failed eligibility check. A settled load error cannot leave an attempt armed for a later retry.
- [x] Direct loads and sidebar-link entries retain first-row focus and immediate ArrowDown navigation on Transactions, Accounts, Categories, Tags, Members, Templates, Recurring, account/group registers, and member drilldowns. `?date=` retains the restored-row behavior from `b6f884e2`; row entry focus causes no scrolling, layout shift, or ring.
- [x] `transaction=` retains detail-panel focus; `?entry=new` retains entry-modal focus; search focused before rows arrive keeps focus; paging does not refocus rows. Existing restoration and activation behavior remains intact.
- [x] `/`, Cmd+L, and Ctrl+L focus and select the available list search and reveal an offscreen field. Slash remains literal inside inputs. Search dispatch remains inert for focused dialog controls, entry modals including non-editable controls, registers, transaction Edit mode, and compact searches parked in a closed Controls sheet; suppressed keys are not consumed.
- [x] Accounts, Categories, Members, and Tags editors no longer globally suppress palette, `n`, or `?`; their existing editable-target and nested-overlay guards still apply. The recurring-definition editor keeps its existing suppression.
- [x] Help retains two `Focus list search` entries in the `List search` group on search-owning pages and omits that group on registers and in Edit mode; group order is unchanged. The existing help journey again asserts `Move row focus`, the redundant recurring visibility assertion is removed, and e2e declarations remain 137 with no new tests.
- [x] Both hooks are exported through the app-shell barrel and all nine cross-feature consumers use it; date-jump entry-row computation is memoized and the roving-row index uses one clamp. Package docs describe settled readiness, the search-only dialog guard, and heading-to-table focus handoff accurately.
- [x] `just pre-commit` and `just test-frontend-e2e` pass before each commit; completion evidence includes representative ordinary browser checks and screenshot paths, and every opened browser session is closed. Inspect `rg -n '^test\(' frontend/tests/e2e` to verify the declaration count; do not count regular-expression `.test(...)` calls.
- [x] Move this plan to `docs/plans/completed/2026-09-17-bmcb-fix-1.md`, commit the move as `docs: complete bmcb fix 1 plan`, and leave the worktree clean.

## Tasks

### Task 1: Wait for settled results and narrow side-panel shortcut suppression

Line references below were verified against this branch and `git diff small-ui-fixes-fleet...HEAD`; they identify implementation anchors, not immutable line numbers.

- [x] Replace `initialResultReady: !loading` at all six sites: `frontend/src/features/members/members-page-content.tsx:175` (`members`), `frontend/src/features/accounts/accounts-tree.tsx:323` (`accounts`), `frontend/src/features/accounts/account-register-table.tsx:198` (`records`), `frontend/src/features/reference/reference-tree.tsx:305` (`leaves`), `frontend/src/features/recurring/recurring-page-content.tsx:375` (`snapshot`), and `frontend/src/features/ledger/transaction-browser.tsx:787` (`transactions`). Use the existing result and error signals, such as `!loading && (data !== undefined || errorMessage !== undefined)`, with the actual local result name; do not use nonempty row count or arrays defaulted to `[]` as readiness. All six already receive `errorMessage`.
- [x] Keep `frontend/src/features/app-shell/use-table-entry-focus.ts:32` as the route-policy adapter and `frontend/src/hooks/use-roving-rows.ts:96` as the attempt owner. The stores at `frontend/src/store/members.ts:24`, `frontend/src/store/accounts.ts:79`, and `frontend/src/store/categories.ts:26` initialize `loading: false` without snapshots; leave those stores unchanged. Ensure the rAF at `use-roving-rows.ts:109` cannot consume the attempt on that pre-request tick and still consumes settled empty/error outcomes without requiring a connected row.
- [x] Remove only the four `data-global-shortcut-blocking-overlay` attributes introduced by `66ad6697`: `frontend/src/features/accounts/accounts-side-panel.tsx:904`, `frontend/src/features/categories/categories-side-panel.tsx:341`, `frontend/src/features/members/members-side-panel.tsx:376`, and `frontend/src/features/tags/tags-side-panel.tsx:300`. Their existing `role="dialog"` roots remain. Keep entry-modal markers and the recurring-editor/global overlay selectors.
- [x] Add a list-search-only predicate beside `resolveVisibleListSearchInput` in `frontend/src/features/app-shell/global-shortcuts.ts:36` that rejects focus inside `[role='dialog']` using `document.activeElement?.closest(...)`. Apply it only to the list-search handler in `frontend/src/features/app-shell/app-shell.tsx:940`, alongside existing editable/overlay guards, before consuming the event. This covers non-editable controls in non-modal entity panels without changing palette, entry-chord, or help predicates.
- [x] Remove the search handler's dead `event.defaultPrevented` check at `app-shell.tsx:943`: the earlier shell capture listener at `:914` only consumes Mod+K. Change `input.focus({ preventScroll: true })` at `:954` to ordinary `input.focus()`, retaining selection and `preventDefault()` for accepted keys. Keep row-focus `preventScroll: true` at `use-roving-rows.ts:128`.
- [x] Remove only the global-shortcut-blocking claims from `frontend/src/features/accounts/PACKAGE.md:36`, `frontend/src/features/categories/PACKAGE.md:19`, `frontend/src/features/members/PACKAGE.md:20`, and `frontend/src/features/tags/PACKAGE.md:16`, preserving their surrounding restoration contracts. Update `frontend/src/features/app-shell/PACKAGE.md:11` to document the search-only focused-dialog guard and remove the prevented-event claim; clarify settled readiness at `:12`. At `:22`, append that heading focus may be followed by the entered table's one entry-focus attempt, retaining the body-only heading guard implemented at `app-shell.tsx:550`. Read the hooks, ledger, reference, and recurring package docs and adjust only any affected contract that is not already represented by their shared-contract links.
- [x] Check representative behavior through ordinary browser interaction: fresh entry and sidebar entry, existing search ownership and deep links, a non-editable entity-panel control suppressing list search while palette/entry/help retain their prior dispatch, the recurring editor's blocking, and a scrolled-away search becoming visible on focus. Preserve all plan-wide protected outcomes without adding timing injection, new tests, or exhaustive route/state matrices. Capture row/search evidence and close opened sessions.
- [x] Run the required formatting and pre-commit validation, then commit as `fix(frontend): settle entry focus readiness and narrow side-panel shortcut guard`.

### Task 2: Simplify hook wiring, date-jump computation, and existing assertions

- [x] Re-export `useTableEntryFocus` and `useListSearchShortcuts` from `frontend/src/features/app-shell/index.ts:1`. Change all six entry-focus imports named in Task 1 and the three search-hook imports in `frontend/src/features/accounts/accounts-page-content.tsx:21`, `frontend/src/features/reference/reference-toolbar.tsx:7`, and `frontend/src/features/ledger/transaction-search-input.tsx:3` to `@/features/app-shell`. Keep imports internal to app-shell relative. Existing cross-feature barrel consumers are account-group content, entity overviews, and settings; do not refactor those consumers.
- [x] Wrap the target lookup plus `findIndex` in `frontend/src/features/ledger/transaction-browser.tsx:777` in `useMemo`, keyed by the date-jump anchor, page, and visible transactions. Preserve anchor-page matching, target fallback, and the undefined index fallback so the restored-date focus from `b6f884e2` remains unchanged.
- [x] Simplify `frontend/src/hooks/use-roving-rows.ts:118` to one clamp, such as `Math.max(0, Math.min(entryFocus.rowIndex ?? activeIndex, rows.length - 1))`. Preserve the optional row lookup, `row?.isConnected` check at `:127`, and empty-result attempt consumption; no extra empty-list branch is needed.
- [x] Replace the structure-coupled heading/parent/`li` text-array assertion at `frontend/tests/e2e/keyboard-shortcuts.spec.ts:26` with the restored `await expect(dialog.getByText("Move row focus", { exact: true })).toBeVisible()` and `await expect(dialog.getByText("Focus list search", { exact: true })).toHaveCount(2)`. Keep the existing slash-focus assertion and the rest of this single journey intact; add no `test(` blocks.
- [x] Remove only `await expect(rows.first()).toBeVisible()` at `frontend/tests/e2e/recurring-page.spec.ts:302`, already covered by `toBeFocused()` at `:296`. Retain the chip assertion at `:301` and the visibility assertion after reload at `:310`.
- [x] Apply `write-package-docs` to touched app-shell, accounts, members, recurring, reference, ledger, and hooks packages; retain their shared contract links and avoid documentation churn for import/memoization-only changes. Confirm normal startup and restored `?date=` focus after the barrel and memoization changes using representative browser interaction, then close any opened sessions.
- [x] Run the required formatting and pre-commit validation, confirm 137 e2e declarations with no new tests, and commit as `chore(frontend): tidy list search shortcut wiring and help assertions`. Complete the plan-wide success criteria and final plan move.
