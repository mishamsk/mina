# Plan: Transactions search and filter bar — Kata issue `0b17`

Add the webui-design filter bar to the Transactions page: a free-text search input plus an "Add filter" menu producing removable typed filter chips, all server-driven through the extended `listTransactions` filter params, with URL-backed state and last-used-state return from the sidebar.

## Plan Context

- Ground truth: `docs/webui-design.md` (Tables and filtering: filter bar pattern + dimensions — reconciliation status omitted until Phase 5; URL state; "Sidebar navigation returns to a page's last-used state"; pickers: hierarchical type-ahead over full FQN paths, include-hidden toggle; loading: previous data stays visible), `docs/frontend-architecture.md` (URL as shareable state; snapshot keys from normalized request params; store rules), `docs/webui-theme-arcade-cabinet.md` (chips, inputs, menus/landmark treatment). Read before starting.
- Current state (line numbers as of this plan's commit):
  - API params all shipped (`types.gen.ts:2042-2088`): repeatable `account_id/category_id/tag_id/member_id/posting_status/transaction_class`, decimal-string `amount_min/max` + `amount_usd_min/max`, `initiated/pending/posted_date_from/to`, `search`; anchor still requires default sort and composes with filters.
  - Data flow: `TransactionPageParams {anchorDate?, limit, offset}` (`api/ledger.ts:25-29`), sort hardcoded (`:40-41`); snapshot keys `transactionPageKey`/`transactionPageRequestKey` (`store/transactions.ts:115-123`) must incorporate a normalized filter signature; URL parsing in `features/ledger/transaction-page-position.ts`; date jump `use-transaction-date-jump.ts` (`cancelDateJump :31-34`) is called on page/page-size changes and must also cancel on filter changes; anchored jumps must thread the active filters.
  - Toolbar: `PageHeader` toolbar slot currently holds only the date-jump control (`transactions-page.tsx:153-184`).
  - Pickers: `EntityPicker` (single-select combobox, substring match over `searchLabel` = FQN) and `EntityMultiPicker` (adds removable chips) in `features/ledger/entity-picker.tsx`; lookups snapshot already contains hidden+tombstoned entities (`api/ledger.ts:59-103`), visibility filtering is caller-side — an include-hidden toggle is a caller-side option-list switch, no new fetches.
  - Primitives: no popover/dropdown wrapper exists; the unified `radix-ui` package (already a dependency, used by tooltip/checkbox/separator) exports `Popover`/`DropdownMenu` — wrap one in `components/ui` per the shadcn pattern rather than hand-rolling. `Badge` + `Button size="icon-xs"` give the removable-chip pattern (see `EntityMultiPicker:231-253`).
  - Sidebar `NavLink`s use static string `to` — "return to last-used state" is unimplemented today.
  - e2e: `expectTransactionsPageUrl` (`transactions-page.spec.ts:60-79`) asserts the URL param set and must learn the new params; request-interception patterns capture outgoing `/api/transactions` query strings; fixture helpers `createTag/createCategory/createMember/createAccount/hideTag/hideCategory` exist inline.
- Operator decisions (do not relitigate):
  - URL schema (lowerCamel, repeatable where multi-value): `account`, `category`, `tag`, `member`, `status`, `class` (repeatable ids/enum values); `amountMin`, `amountMax`, `amountUsdMin`, `amountUsdMax`; `initiatedFrom/To`, `pendingFrom/To`, `postedFrom/To`; `q` for search. Invalid URL values are dropped silently on parse (URL is user-editable); everything valid maps 1:1 onto the API params. Filter/search changes reset `page` to 1 and `cancelDateJump()`; all writes stay functional `setSearchParams`.
  - Filter bar UI in the existing toolbar slot: search input (debounced ~300ms, labeled, themed like the date-jump input; excluded from the global "n" hotkey guard which already ignores inputs) + "Add filter" menu button (new `components/ui` popover/dropdown wrapper over radix) listing the dimensions; picking a dimension opens its editor inline in the popover: entity dimensions use the shared pickers (multi-select for account/category/tag/member) fed from the lookups snapshot with an include-hidden toggle (hidden entities render with the eye-off indicator per design); posting status and class are checkbox lists of the typed enum values; amount and date dimensions are min/max // from/to paired inputs (native date inputs; decimal text inputs validated with the API's pattern).
  - Chips row under the toolbar: one chip per selected entity/enum value (label = leaf name or enum label, FQN tooltip for entities), one chip per active range (e.g. "Amount 10–50", "Initiated 2026-05-01–2026-06-01"), each removable (X button, accessible label), plus a "Clear all" affordance when 2+ chips. Chips follow the theme chip treatment.
  - Server-driven only: every change maps to new request params; snapshot keys include the normalized filter signature (sorted ids, canonical ordering) so pages cache per filter set; previous rows stay visible while refetching (existing busy pattern, no skeleton flash) — do not invalidate the page cache on filter change, the new key namespace is enough.
  - Date jump composes with filters (thread filter params through `jumpToTransactionDatePage`).
  - Last-used-state return: a small in-memory store field capturing the Transactions page's latest `location.search`; the sidebar Transactions `NavLink` targets `{pathname: "/transactions", search: lastUsed}`. Session-scoped only (no IndexedDB); other pages unaffected.
  - No sort UI (sort stays hardcoded); no reconciliation-status dimension.
- Preserve, do not regress: pagination/anchor e2e, detail deep links (`?transaction` param must coexist with filter params), entry panel flows, existing keyboard behavior, suites green.
- Feature delivers the webui-design filter bar: update `PROJECT_STATE.md` in the final commit.

## Tasks

### Task/Commit 1: Filter params plumbing + URL schema + search input

- [x] Extend `TransactionPageParams`/`TransactionsPageParams`, `fetchTransactionPage`, and both snapshot key builders with the full filter set (normalized signature in keys); thread filters through the anchored-jump path; `cancelDateJump` on filter change.
- [x] URL parse/serialize helpers in `transaction-page-position.ts` for the schema above (drop invalid values silently; reset page on change).
- [x] Search input in the toolbar (debounced `q`, labeled, themed); requests carry `search`; previous rows stay visible while refetching.
- [x] e2e: typing a memo/counterparty fragment filters the list server-side (assert the outgoing request carries `search` and the URL carries `q`); URL with `q` deep-links to filtered results; `expectTransactionsPageUrl` updated.
- [x] Verification
  - [x] `just test` passes
  - [x] `just pre-commit` passes
  - [x] `just test-frontend-e2e` passes
  - [x] Commit changes

### Task/Commit 2: Add-filter menu, chips, pickers, ranges

- [x] `components/ui` popover/dropdown wrapper (radix, theme treatment) + the Add-filter menu with per-dimension editors per the operator decisions (shared pickers with include-hidden toggle; enum checklists; amount/date range inputs).
- [x] Removable chip row (per-value chips, range chips, Clear all); chips reflect and write URL state; all keyboard operable per design.
- [x] e2e: add each dimension type at least once (entity multi-value any-of, enum, amount range, a date range), assert outgoing request params and URL round-trip (deep-link a multi-filter URL); remove chips and Clear all; include-hidden toggle surfaces a hidden entity in the picker (mirror the hidden-tags precedent); composed filters + pagination + date jump still work.
- [x] Verification
  - [x] `just test` passes
  - [x] `just pre-commit` passes
  - [x] `just test-frontend-e2e` passes
  - [x] Commit changes

### Task/Commit 3: Last-used-state sidebar return + PROJECT_STATE

- [x] In-memory store capture of the Transactions page's latest search string; sidebar Transactions NavLink returns to it; navigating away and back via the sidebar restores filters/search/page.
- [x] e2e: apply filters, navigate to Status via sidebar, return via sidebar Transactions — URL state restored.
- [x] Update `PROJECT_STATE.md`.
- [x] Verification
  - [x] `just test` passes
  - [x] `just pre-commit` passes
  - [x] `just test-frontend-e2e` passes
  - [x] Commit changes

## Final Verification

- [x] Final verification handled by the original implementation/review flow.
- [x] Commit final changes
- [x] Review-loop is owned by the external review driver for this pass.
- [x] Move this plan to `docs/plans/completed/`
