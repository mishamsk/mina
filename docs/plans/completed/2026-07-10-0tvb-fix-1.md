# Plan: 0tvb review fix 1 — align toolbar geometry spec with labeled day-navigation buttons (`0tvb`)

The final `0tvb` commit converted the Previous/Next day controls from 36×36 icon buttons to labeled buttons (the `docs/webui-design.md` compliant form — controls are never icon-only outside the rail and row actions), but left the toolbar geometry e2e asserting the icon-era 36px widths. The committed suite fails at HEAD. Fix the spec to the labeled-button reality and prove the whole suite green.

## Plan Context

- Do not run review-loop.
- Confirmed failure: `frontend/tests/e2e/transactions-page.spec.ts:981` (`expect(previousDayButtonBox?.width).toBe(36)` receives ~150.8) and `:983` for Next day — "transactions filter toolbar keeps a stable inline trigger geometry" fails on chromium and webkit at HEAD.
- Decision (operator): keep the labeled buttons (`size="lg"`, visible "Previous day"/"Next day" text, `frontend/src/pages/transactions-page.tsx:326-338,360-373`) — they are the design-doc-compliant form. Do NOT revert to icon-only buttons.
- Fix: in the geometry test, replace the two width===36 assertions for the day-navigation buttons with assertions that fit labeled buttons (keep height===36 and the control-row y-alignment; a sanity width>36 or no width assert is fine). Do not touch the Add-filter trigger's 36×36 assertions or any other test.
- Protect — do not regress: everything else on this branch (day-step behavior, focus restoration, local-date arithmetic, existing e2e); no source-file changes; no doc changes.
- Kata `0tvb` is closed with a verification comment that claimed a green suite; append a correcting comment with the fixed state and fresh evidence. Do not reopen.

## Tasks

### Task/Commit 1: Fix the geometry spec and verify the full suite

- [x] Update the two day-navigation width assertions in `frontend/tests/e2e/transactions-page.spec.ts` (~:981,:983) to match labeled buttons; keep height and alignment assertions.
- [x] Run the FULL `just test-frontend-e2e` and paste the actual pass count into the Kata comment — do not report success without a fresh complete run.
- [x] Comment the correction and evidence on Kata `0tvb`.
- [x] Verification
  - [x] `just test` passes
  - [x] `just pre-commit` passes
  - [x] `just test-frontend-e2e` passes (full suite, fresh run)
  - [x] Commit changes

## Final Verification

- [x] `just test-frontend-e2e` passes (full suite)
- [x] Commit final changes
- [x] Move this plan to `docs/plans/completed/`
