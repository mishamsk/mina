# Plan: 0tvb review fix 2 — revert unauthorized detail-panel repositioning (`0tvb`)

Commit `f5a7af81` included an out-of-scope product change: the transaction detail panel's container moved from `top-4` to `top-48` (`frontend/src/features/ledger/transaction-detail-panel.tsx:593`), breaking the app-wide side-panel inset pattern (all side panels sit at `top-4 right-4 bottom-4` — accounts, members, categories, tags panels). Revert it. If any e2e test then fails because the panel overlays the toolbar, fix the TEST (close the panel or interact in the right order) — never reposition product UI to satisfy a test.

## Plan Context

- Do not run review-loop.
- The only allowed source change: revert `top-48` back to `top-4` in `transaction-detail-panel.tsx:593`. No other source edits.
- If `just test-frontend-e2e` fails after the revert, the failure is in test interaction (e.g., a day-navigation or detail-panel spec clicking toolbar controls while the fixed panel overlays them). Fix it in `frontend/tests/e2e/*.spec.ts` only: close the panel (Escape / close button) before toolbar interactions, or reorder steps. Product behavior is ground truth: the panel is `top-4` and non-modal per `docs/webui-design.md` (side peek panels, outside clicks pass through).
- Run the FULL `just test-frontend-e2e` fresh and record the actual pass count on Kata `0tvb`.
- Protect: everything else on the branch.

## Tasks

### Task/Commit 1: Revert and verify

- [x] Revert `transaction-detail-panel.tsx` to `top-4`.
- [x] Run the full suite; if a spec fails from panel overlay, fix that spec's interaction order only.
- [x] Append the correction and fresh full-suite evidence to Kata `0tvb`.
- [x] Verification
  - [x] `just test` passes
  - [x] `just pre-commit` passes
  - [x] `just test-frontend-e2e` passes (full suite, fresh run)
  - [x] Commit changes

## Final Verification

- [x] `just test-frontend-e2e` passes (full suite)
- [x] Commit final changes
- [x] Move this plan to `docs/plans/completed/`
