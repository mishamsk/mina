# Plan: y7yk balances API — operator review fix (fix plan 1) — Kata issue `y7yk`

One documentation-contract gap from the operator audit. Implementation-only, no behavior change.

## Plan Context

- Do not run review-loop.
- Accepted as-is (do not change): the handler computing the as-of local civil date from the injected clock (deliberate plan decision — clocks live at boundaries); the httpapi-local `Clock` interface (consumer-defined interface is idiomatic under the inward-import rule).
- Protect — do not regress: all suites; API schema; live behavior verified (USD equivalents incl. EUR conversion via stored amount_usd; credit_limit on card rows only).
- Scope exclusions: nothing beyond the doc items below.

## Tasks

### Task/Commit 1: Document the CurrentByAccounts contract

- [x] `internal/services/creditlimits/creditlimits.go:146` — expand the `CurrentByAccounts` doc comment with the contract callers depend on: tombstoned rows excluded; the latest `effective_date <= as-of` row wins (deterministic tie-break); accounts with no applicable limit are absent from the returned map (absence ≠ zero).
- [x] If `internal/services/creditlimits` has a `PACKAGE.md` with an Implicit Contracts section, reflect the same semantics there in one line; create nothing new if the method doc suffices per the package-doc policy.
- [x] Verification
  - [x] `just test` passes
  - [x] `just pre-commit` passes
  - [x] Commit changes

## Final Verification

- [x] `just test` passes
- [x] `just pre-commit` passes
- [x] Commit final changes
- [x] Move this plan to `docs/plans/completed/`
