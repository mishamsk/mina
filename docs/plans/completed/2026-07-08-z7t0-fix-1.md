# Plan: z7t0 fix 1 — reference search eats spaces while typing; stale reference PACKAGE.md

Fix two operator-review findings: the reference toolbar's controlled search input trims the URL-backed value on every keystroke (typing `Uncle Bob` yields `UncleBob` — first user-facing on the Members page where names contain spaces), and `features/reference/PACKAGE.md` still documents the flat-list ownership that moved into `features/members`.

## Plan Context

- Do not run review-loop.
- Frontend-only. No ground-truth doc edits, no API changes.
- Defects (as of commit `f49a0a6`):
  1. `frontend/src/features/reference/reference-toolbar.tsx:55-62` — `setSearch` writes `nextSearch.trim()` to the URL on each keystroke while the input is fully controlled by the URL value, so an interior-space-in-progress (trailing space before the next word) is stripped mid-typing. Affects Categories, Tags, Accounts-style search too, but Members (multi-word names) makes it user-facing. Fix so typing values with spaces works naturally (e.g. trim only for filtering/comparison or when committing, not on every controlled-value round-trip); leading/trailing whitespace must still not linger in the shareable URL after typing stops in a way that breaks filtering — matching by trimmed value is fine.
  2. `frontend/src/features/reference/PACKAGE.md` lines about the flat reference list ("Flat reference lists use URL-backed `q` only…", "Owns: … tree/list derivation …") are stale — the flat list implementation lives in `features/members` now. Make the doc match reality (reference owns the tree/toolbar pieces; flat lists are page-owned).
- Protect — do not regress: URL-backed `q` behavior and its e2e on categories/tags/members (search still filters by trimmed, case-insensitive match; URL stays shareable); everything else on these pages.
- Scope exclusions: nothing else — do not touch stores, panels, or e2e beyond what the two fixes need.

## Tasks

### Task/Commit 1: search input space handling + doc fix

- [x] Fix the search-typing trim behavior in the reference toolbar per Plan Context (verify on the Members page by typing a two-word value character-by-character; matching stays trimmed/case-insensitive).
- [x] e2e: add a keystroke-level search assertion (use `pressSequentially`, not `fill`) on the members spec: typing a name with a space filters to the expected row and the input retains the space.
- [x] Update `frontend/src/features/reference/PACKAGE.md` to match current ownership.
- [x] Verification
  - [x] `just test` passes
  - [x] `just pre-commit` passes
  - [x] `just test-frontend-e2e` passes
  - [x] Commit changes

## Final Verification

- [x] `just test` passes
- [x] `just pre-commit` passes
- [x] `just test-frontend-e2e` passes
- [x] Commit final changes
- [x] Move this plan to `docs/plans/completed/`
