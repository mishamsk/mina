# Plan: 0b17 filter bar — operator review fixes (fix plan 1) — Kata issue `0b17`

Fix a confirmed Escape-layering defect and unify duplicated label/pattern sources. The filter bar itself is verified working live (search, chips, URL round-trip, sidebar last-used return).

## Plan Context

- Do not run review-loop.
- CONFIRMED DEFECT (operator live test, 1920px viewport, Chromium): with the transaction detail panel open AND the Add-filter popover open, pressing Escape closes BOTH — the popover's React synthetic `onKeyDown` `stopPropagation` (`transaction-filter-controls.tsx:714-718`) does not suppress the detail panel's document-level bubble listener (`transaction-detail-panel.tsx:378`) across the Radix portal boundary. Expected: first Escape closes only the popover; second Escape closes the detail panel.
- Fix mechanism (operator decision): use event-level coordination that works across portals — in the popover's Radix `onEscapeKeyDown`, close the popover and `event.preventDefault()`; the detail panel's document listener ignores Escape events with `defaultPrevented` (Radix's dismissable-layer document listener runs before the panel's bubble listener). Apply the same guard for the inner entity-picker dropdown layering if needed. No fragile synthetic-event `stopPropagation` across portals.
- Other findings (operator audit): class labels inconsistent — the checklist uses a local Title-Case `classLabels` map (`transaction-filter-controls.tsx:83-92`) while chips use `transactionClassLabel` (UPPERCASE compact) — user sees "Currency exchange" in the picker but "Class EXCHANGE" on the chip; posting-status title-casing duplicated from `transaction-detail-panel.tsx:70`; `decimalPattern` regex duplicated (`transaction-filter-controls.tsx:68`, `transaction-page-position.ts:34`).
- Protect — do not regress: all e2e; URL schema and round-trip; chips behavior; debounced search; date-jump signature guard; entry-panel picker flows; keyboard operability.
- Scope exclusions: no chip-render refactor beyond what the label unification touches; no shared Input primitive (pre-existing pattern); nothing else.

## Tasks

### Task/Commit 1: Escape layering fix with e2e

- [x] Implement the preventDefault/defaultPrevented Escape coordination described above; first Escape closes only the topmost layer (picker dropdown → popover → detail panel), pressing Escape again proceeds down the stack.
- [x] e2e: open a transaction detail, open the Add-filter popover, press Escape — popover closes, detail stays open; Escape again closes the detail. Use a viewport wide enough that the toolbar is clickable with the panel open (the existing wide-viewport precedent).
- [x] Verification
  - [x] `just test` passes
  - [x] `just pre-commit` passes
  - [x] `just test-frontend-e2e` passes
  - [x] Commit changes

### Task/Commit 2: Single label and pattern sources

- [x] One human-readable label source for transaction classes used by BOTH the class checklist and the chips (place it with the existing format helpers; the compact UPPERCASE form stays for table cells if used there).
- [x] Hoist the posting-status label helper to the shared format module and reuse in the detail panel and filter controls; export one decimal-pattern constant (natural home: `models/transaction-filters.ts`) reused by the editor and URL parser.
- [x] Verification
  - [x] `just test` passes
  - [x] `just pre-commit` passes
  - [x] `just test-frontend-e2e` passes
  - [x] Commit changes

## Final Verification

- [x] `just test` passes
- [x] `just pre-commit` passes
- [x] `just test-frontend-e2e` passes
- [x] Commit final changes
- [x] Move this plan to `docs/plans/completed/`
