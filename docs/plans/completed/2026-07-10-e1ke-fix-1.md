# Plan: e1ke review fix 1 — make the mixed-amount layout test non-interfering (`e1ke`)

The new layout regression test breaks a later spec in full-suite runs: with this branch, `just test-frontend-e2e` fails `[webkit] transactions-page.spec.ts:3591 "spend entry escalates to matching journal records"` consistently (operator reproduced twice, clean ports), while the same suite is green on the base branch and the failing test passes in isolation. The prior session dismissed this as "unrelated timing flakes" — it is not: it is caused by this branch's additions. Root-cause and fix within the test layer so the full suite is deterministically green.

## Plan Context

- Do not run review-loop.
- Evidence: full suite on this branch → 133 passed/1 failed, then 132 passed/1 failed (same webkit test); base branch `ui-stage-4` → 132 passed, green; the failing test passes in isolation on both browsers. Ports were verified clear before every run (no stale servers).
- Prime suspects in the new test ("mixed amount chips stay inside the amount column where member first appears"): it creates a persistent mixed transaction against the same demo entities the escalation test uses (`cash:Wallet`, `Entertainment:Books`), leaves it in the dataset, changes viewport sizes repeatedly, and queries with `pageSize=50`. Investigate the actual interference mechanism (fixture visibility in the escalation test's expectations, picker contents, server-side ordering, or load-induced webkit timeout) before changing anything — the fix must address the cause, not add waits.
- Allowed changes: test files only (`frontend/tests/e2e/*.spec.ts`) — e.g., isolate fixtures (unique accounts/categories created via API instead of demo entities where practical), delete the created transaction at the end of the layout test, or otherwise make the new test leave no observable footprint. Do NOT touch product source or loosen the failing test's assertions.
- The layout coverage itself must remain intact: breakpoint straddling (below/at/above member reveal), containment assertions, single-row chip.
- Prove determinism: run the FULL `just test-frontend-e2e` twice consecutively, both green, and record both pass counts on Kata `e1ke`.

## Tasks

### Task/Commit 1: Root-cause and fix the interference

- [x] Identify the interference mechanism and record it in the Kata comment (one paragraph, file:line).
- [x] Fix within test files only; keep all layout assertions.
- [x] Run the full suite twice consecutively; both green; record both counts on Kata `e1ke`.
- [x] Verification
  - [x] `just test` passes
  - [x] `just pre-commit` passes
  - [x] `just test-frontend-e2e` passes twice consecutively (full suite, fresh runs)
  - [x] Commit changes

## Final Verification

- [x] `just test-frontend-e2e` passes (full suite)
- [x] Commit final changes
- [x] Move this plan to `docs/plans/completed/`
