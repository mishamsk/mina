# Plan: Repair transaction entry focus and template shortcut — cwxv fix 1

## Goal

Fix the eight audited defects on `cwxv-entry-first-empty-focus` while preserving the entry behavior and visual design in [the archived design plan](completed/2026-09-17-cwxv-entry-first-empty-focus.md). Make modal `Mod+L` consumption independent of template request timing, replace picker DOM control messages with React refs, and simplify initial focus without moving the page or pinned frame. This is an implementation-only follow-up; Kata `cwxv` is already closed.

## Constraints

- Do not run review-loop.
- `docs/FRONTEND-TESTING.md` governs. Do not add e2e tests; reviewers must not request coverage; fold any missing assertion into an existing journey. Net e2e `test(` count stays at most 137 (`rg -n '^test\(' frontend/tests/e2e`).
- Frontend-only; no Go, OpenAPI, backend suites, persistence-shape changes, or new dependencies. Do not reopen or close Kata `cwxv` again. Preserve the archived plan.
- Do not modify the global list-search handler, `globalShortcutGroup`, or the List search help group. Leave `docs/webui-design.md` unchanged: its Keyboard clause at line 177 still correctly says the chord opens Start from a template when the active create tab has compatible templates; consuming an otherwise inert chord refines that rule without contradicting it.
- Run `just pre-commit` and `just test-frontend-e2e` before each implementation commit; use Justfile recipes only for formatting, checks, tests, and development startup. No `just test` or `just test-integration`. The docs-only plan-move commit requires no tests or broad validation. Use `just prose-fmt` for implementation documentation; that recipe excludes `docs/plans/`.
- Use `write-package-docs` for every package touched; update only affected contracts in `frontend/src/features/ledger/PACKAGE.md` alongside their implementation. Keep changes within the ledger feature and, only if necessary, existing frontend journeys.
- Preserve ordinary pickers' lazy behavior and the entry panel as the only consumer of programmatic open. No synthetic DOM events or `data-*` control attributes may remain for picker focus/open; read-only selection metadata may remain for field emptiness.
- Close every `agent-browser` session opened, including named sessions, before finishing. Report screenshot paths for the re-verified modal: Overview `n` → Date focused, Transactions `n` → Funding account focused, and `Mod+L` → template picker open.
- Browser evidence uses normal UI controls and visible results. Do not add timing/failure injection, request synchronization, fixed waits, exact-geometry assertions, or a new automated state/route matrix; inspect asynchronous guards in code and use the existing journeys for browser evidence.

## Success Criteria

- [x] All eight findings below are addressed. With a rendered, enabled create template picker and no confirmation dialog, plain `Mod+L` is consumed on every tab, including Exchange, whether root availability is unknown, unavailable, or available. Only available opens/focuses the picker; unknown/unavailable leave focus and popup state alone. Confirmation dialogs, disabled/saving/clearing states, and edit/split launches without a picker leave the chord unconsumed.
- [x] Programmatic focus uses React handles, cannot leave suppression behind after failed focus, opens an already-focused picker, and shares selected-option seeding with user focus. Clicking an already-focused picker still opens it. Root preload has one enabled predicate, retains/reuses current reads across saving transitions, and has one lifecycle reset owner.
- [x] Initial focus remains one-shot per launch: unresolved refs can retry, successful focus settles the attempt, a settled empty form can finish without a target, and user interaction or conflict preservation cancels further initial focus for that session. No focus is delivered late after a tab change, confirmation, replacement conflict, or superseding launch.
- [x] Preserve the operator-verified targets: Overview `n` → Date without a popup; Transactions `n` → Funding account; `n i` → Income amount; `n t` → Transfer amount; `n a` → first Advanced record account; Edit and unseeded Templates Use → Date; restored saved draft → next empty merchant account; compact Transactions `n` → Funding account without page scroll. Preserve split/duplicate Date focus, all-populated first-field fallback, optional-field emptiness, and save-and-add-another Date focus.
- [x] Preserve Spend `Mod+L`, including from Memo and after Esc while the picker remains focused; Esc closes its popup without closing the modal. Exchange never opens through the chord. `?` from a non-editable modal control lists “Start from a template” under Transaction entry, help closes back into the modal, and modal closure restores its opener. Draft and template-replacement choices remain intact.
- [x] Existing phone-journey focus/click assertions and the command-palette setup click remain valid; no new e2e tests are added. `just pre-commit` and `just test-frontend-e2e` pass before each implementation commit, and the e2e declaration count remains at most 137.
- [x] Completion evidence includes browser findings, limitations, and screenshot paths `build/cwxv-fix-1/overview-entry-focus.png`, `build/cwxv-fix-1/transactions-entry-focus.png`, and `build/cwxv-fix-1/template-picker-open.png`. Screenshots show the existing theme ring, stable frame, correct focus, and expected popup state; every opened browser session is closed.
- [x] Move this plan to `docs/plans/completed/2026-09-17-cwxv-fix-1.md`, commit the move as `docs: complete cwxv fix 1 plan`, and leave the worktree clean.

## Tasks

### Task 1: Consume modal Mod+L consistently and replace the picker focus channel

Verified against `git diff small-ui-fixes-fleet...HEAD` and current source; references below use the full paths `frontend/src/features/ledger/entry-panel.tsx` and `frontend/src/features/ledger/entity-picker.tsx` abbreviated to their filenames.

- Findings 1 and 6: `entry-panel.tsx:5191–5218` puts the four confirmation guards before the chord and requires both `templateOptionLoader` and available root state before `preventDefault()`. `entry-panel.tsx:3261–3270` omits the loader on Exchange, but `:5306–5335` still renders the picker whenever there is no replacement. Eligibility must follow rendered/enabled state, not loader presence or launch truthiness; duplicate uses the create picker despite its launch identity.
- Findings 1 and 8: `entity-picker.tsx:657–665` resets availability on mount/loadKey and cleanup and redundantly optional-chains after a guard. Failure at `:611–623` reports unknown until a successful retry. `entry-panel.tsx:5314` already keys the picker by active tab and lifecycle; `:4782` and `:5102` increment that lifecycle after saves. A tab change remounts through the key itself.
- Finding 3: `entity-picker.tsx:75–99` writes focus intent into the DOM and dispatches `mina:entity-picker-open`; `:783–797` listens, `:1246–1277` consumes the intent and seeds user-focus selection, and `:1281–1285` opens on click without that seeding. `entry-panel.tsx:3169–3179` and `:3523` are the helper's only consumers. `EntityPicker` at `entity-picker.tsx:1837` and `EntityMultiPicker` at `:1865` must both carry the replacement handle.
- Finding 4: `entry-panel.tsx:5317` and `:5324` duplicate enabled state with opposite predicates. `entity-picker.tsx:501–503` folds preload into load eligibility, and `:645–655` lists both preload and eligibility as effect dependencies; a saving toggle can cancel/reissue root work independently of the keyed remount.
- Protected evidence: `frontend/tests/e2e/transactions/entry.spec.ts:533–536` checks Funding account focus and click-to-open, and `frontend/tests/e2e/command-palette.spec.ts:200` clicks the template picker before its expanded assertion. Commit `59b330ca` introduced the already-focused click behavior. `frontend/src/features/ledger/entry-shortcuts.ts:24–27` owns the entry help detail. The current e2e declaration count is 137.

Design spec: retain the existing header picker, white modal surface, pinned header/footer, compact takeover, and unchanged field/tab order. Initial focus opens no popup; eligible explicit open uses the existing picker placement and keyboard behavior. Preserve the standard 2px `--ring` outline with 2px offset, without new component colors or layout changes on focus, hover, or modifier state. Unknown/unavailable availability, including Exchange, means consume-only while enabled; confirmations and absent/disabled pickers retain native chord behavior. This supersedes only the archived plan's unconsumed-unknown/unavailable/Exchange rule.

- [x] Derive one template-picker disabled/enabled condition, shared by rendering props and chord eligibility, covering draft readiness, saving (including draft operations), and clearing. After the existing confirmation guards, consume unmodified `Mod+L` whenever the create picker is mounted and enabled; then use only positive current-lifecycle availability to decide whether to focus/open. Remove the redundant loader check. Preserve text-input handling and no-Alt/no-Shift matching.
- [x] Replace `focusEntityPicker`, the dataset intent, and event listener/dispatch with a typed `useImperativeHandle` ref exposing `focus({ open?: boolean, preventScroll?: boolean })`. Forward it through single and multi-pickers; connect template focus directly and initial form focus through an entry-owned ref registry keyed by the actual input/field identity, with unmount cleanup. Keep the form's DOM-order discovery; do not introduce a global registry or change ordinary callers.
- [x] Share the existing focus-open selection/text and active-row seeding between genuine user focus and explicit open. Suppress the synchronous focus-open path only for the duration of the imperative focus call, clearing suppression even if focus fails; open explicitly once only after focus lands, including when already focused. Preserve click-to-open, autoFocus suppression, normal later focus, arrows, and Esc. Report actual focus success to the entry panel or verify `document.activeElement` there; a picker match alone is not success.
- [x] Make root preload an opt-in mode derived internally from the picker's own enabled state, rather than passing the inverted disabled expression. Reuse the existing bounded root result/in-flight request for its request identity; disable/re-enable must not issue an extra identical root search or erase a valid result. A keyed new lifecycle may make its one necessary root read. Keep existing retry, query/scope changes, exclusion keys, stale-response rejection, and ordinary lazy loading; do not add a panel fetch or a general cache.
- [x] Keep keyed picker identity as the single availability-reset owner and remove the redundant `loadKey` reset effect at `entity-picker.tsx:657–665`, including its stacked guard. Scope published availability to the current tab/lifecycle identity so old callbacks and no-loader Exchange cannot inherit available state, including before passive effects run. Unknown on request start/failure remains request status, not a second lifecycle-reset mechanism. Remove the redundant template `loadKey` if identity is already supplied by the remount; retain unrelated reusable search-key behavior.
- [x] Use `write-package-docs` to replace the ledger contract that consumption waits for positive availability and document the ref focus, single lifecycle reset, and preload-reuse invariants. Update the entry shortcut detail to distinguish opening when templates exist from consuming the chord while create is enabled; keep the label and Transaction entry group. Do not change the List search group or `docs/webui-design.md`.
- [x] Manually verify Spend opening from Memo, Esc/reopen while still focused, click-to-open, Exchange consume-only, confirmation/absent-picker behavior, help and opener restoration. Inspect unknown/failed-read paths and save/remount request ownership without adding timing tests. Retain existing phone and palette journeys; fold any necessary visible assertion into an existing journey only.
- [x] Close opened browser sessions, run `just prose-fmt`, then `just pre-commit` and `just test-frontend-e2e`; inspect the diff and e2e declaration count before committing.
- [x] Commit as `fix(ledger): consume modal Mod+L and replace picker focus channel`.

### Task 2: Simplify initial-focus scrolling and settle the launch guard correctly

Verified evidence:

- Finding 2: `entry-panel.tsx:2905–2937` walks ancestors, hardcodes a 4px ring margin, and lacks a body-containment check. Its caller at `:3514` invokes it even when the field is already visible. The scroll body is `:5359–5365`; the panel at `:5189` and fixed modal at `frontend/src/features/ledger/entry-modal.tsx:290` own the pinned frame.
- Finding 5: `entry-panel.tsx:2855` includes native `select`, `:2885–2887` accepts `HTMLSelectElement`, and `:2898` checks its value. There is no native `<select>` in the form. The actual Radix triggers at `:5714` and `:5822` represent populated origin and settlement choices, not empty native selects.
- Finding 7: `entry-panel.tsx:3493–3495` marks the attempt with no body; `:3510` marks before checking the resolved target or focus success. `:3482–3487` clears conflict preservation without retiring the session. Conflict recovery sets that flag at `:4497`; launch generation changes at `:3275–3281`. The effect at `:3468–3542` must retain its draft/template readiness and confirmation gates.

- [x] Remove the native-select selector and `HTMLSelectElement` branches. Keep input/textarea and entity-picker emptiness based on actual values/selection IDs, including multi-picker group selections; zero and unresolved selected IDs count as populated. Do not add Radix Select emptiness behavior: its origin/settlement choices already have values. Preserve hidden/inert/disabled/read-only exclusions and first-candidate fallback.
- [x] Delete `scrollFieldIntoEntryBody`. Resolve a connected target contained by the active `entryScrollRegionRef`, and check whether it is already within the body's visible area. Only when it is outside that area, call `target.scrollIntoView({ block: "nearest", inline: "nearest" })`; then focus with `preventScroll: true` using Task 1's handle for pickers. Use a Tailwind scroll-margin utility if needed to reveal the existing ring, not JS ring-margin arithmetic. Preserve nested Advanced horizontal visibility. Reject targets outside the body; ensure native scrolling never scrolls the page or pinned frame and already-visible targets cause no scroll.
- [x] Set `initialFocusAttemptedSessionRef` only after focus lands or the ready, mounted launch has settled with no candidates. Missing body/Date refs or a detached target are transient, not completion: arrange a cancellable retry tied to mounting/readiness for the same launch, rather than relying on an unrelated dependency change or polling indefinitely. Before revealing and before focusing, recheck session identity, target ownership, readiness, confirmation state, and whether user interaction superseded the attempt.
- [x] Retire the session's initial-focus work explicitly when preserving conflict/user focus, and cancel any queued frames; do not merely clear `preserveFocusOnReplacementChangeRef` and leave a later attempt possible. Keep confirmation deferral before an untouched launch's first focus, distinguish it from conflict cancellation, and preserve existing save-and-add-another Date focus. Tabs, availability responses, lookup refreshes, and ordinary draft edits must not restart a settled/cancelled launch.
- [x] Use `write-package-docs` to update the affected initial-focus lifecycle and scroll-ownership contracts in the ledger `PACKAGE.md` without adding a file tour or unrelated documentation.
- [x] Re-verify the protected focus outcomes in Success Criteria through short browser checks, including compact shell and a restored draft, plus representative offscreen/Advanced visibility. Confirm initial picker focus stays closed, later click opens it, and focus/ring visibility never moves the page or pinned frame. Inspect failed-focus and conflict cancellation paths in code without adding isolated regression tests.
- [x] Capture the three required screenshots after the final changes, immediately after Overview `n`, Transactions `n`, and successful modal `Mod+L`; report paths and limitations, and close every opened browser session. Run `just prose-fmt`, then `just pre-commit` and `just test-frontend-e2e`; inspect the diff and confirm the e2e declaration count is at most 137.
- [x] Commit as `chore(ledger): simplify entry initial focus helpers`, then complete the plan-move step in Success Criteria.
