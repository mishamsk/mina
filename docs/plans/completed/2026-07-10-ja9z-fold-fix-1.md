# Plan: ja9z review fix 1 — cover per-row fold mixed state and drop dead table containers (`ja9z`)

Close the review findings on the fit-based row-action folding branch: the per-row fold behavior (each row folds only when its own action cluster does not fit the actions cell) is now the documented rule in `docs/webui-design.md`, but its most distinctive consequence — adjacent Accounts rows with different action counts folding at different widths — has no test coverage; and the old per-table fold removal left dead named-container declarations behind.

## Plan Context

- Do not run review-loop.
- Ground truth (already amended, do not edit docs): `docs/webui-design.md` — button-class actions fold per row when the actions cell cannot fit that row's full action cluster; toggles never fold.
- Review findings to fix (file:line evidence from the operator audit):
  1. Mixed fold state untested. Accounts action-count populations (`frontend/src/features/accounts/accounts-tree.tsx:627-722`): group-only rows = 2 actions, leaf accounts = 4, accounts that are both leaf and group = 5. With fold thresholds 52/108/136px there is a band (actions-cell inner width in [108, 136)) where 5-action rows fold to the overflow (⋯) while adjacent 4-action leaves stay unfolded. `frontend/tests/e2e/reference-row-actions.spec.ts` never exercises this.
  2. Dead CSS: `.accounts-table-scroll` and `.reference-table-scroll` named-container declarations (`frontend/src/styles.css:212-219`) have zero remaining `@container accounts-table/reference-table` rules after the old breakpoint queries were removed.
- Protect — do not regress: everything verified sound in review — fold threshold math (N*24 + (N-1)*4, strict `<`), the `row-actions` named container and its per-count queries scoped to `:is(.accounts-table, .reference-table)`, centered folded cluster, hover/focus reveal, persistent toggle visibility, keyboard operability, transactions-table and account-register-table rules and layout, accounts column rebalance and skeleton grid, and all existing e2e suites including `reference-row-actions.spec.ts` and `reference-table-layout.spec.ts`.
- Scope exclusions: no behavior changes to folding, centering, or thresholds; no doc edits; no changes outside the e2e spec and the dead CSS declarations; do not touch `PROJECT_STATE.md`.
- Kata issue: `ja9z` (already closed; add a comment with the fix evidence, do not reopen).

## Tasks

### Task/Commit 1: Mixed-state e2e coverage and dead container cleanup

Add browser coverage for the per-row fold divergence on Accounts and remove the two dead named-container declarations.

- [x] Extend `frontend/tests/e2e/reference-row-actions.spec.ts` with an Accounts mixed-state scenario: fixtures that yield at least one 5-action row (an account that is both a leaf and a group parent) next to 4-action leaf rows; at a viewport where the actions cell inner width falls in the [108, 136) band, assert via the measured fit predicate that the 5-action row is folded (overflow visible, buttons hidden, toggles visible, cluster centered) while a 4-action row in the same table stays unfolded with all buttons revealed on hover/focus.
- [x] In the same spec, assert both rows satisfy the per-row fit predicate: folded if and only if that row's measured cluster requirement exceeds the measured cell space.
- [x] Remove the dead `.accounts-table-scroll` and `.reference-table-scroll` container declarations from `frontend/src/styles.css`; verify no `@container accounts-table` or `@container reference-table` rules remain and no other rule references those container names.
- [x] Keep tests at browser boundaries; reuse the spec's existing fixture and measurement helpers; fixture names sort after demo data.
- [x] Comment fix evidence on Kata `ja9z`.
- [x] Verification
  - [x] `just test` passes
  - [x] `just pre-commit` passes
  - [x] `just test-frontend-e2e` passes
  - [x] Commit changes

## Final Verification

- [x] `just test` passes
- [x] `just pre-commit` passes
- [x] `just test-frontend-e2e` passes
- [x] Commit final changes
- [x] Move this plan to `docs/plans/completed/`
